import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class CRTInviteDatePicker extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                CRTInviteDatePicker
      </div>
        )
    }
}
