import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class CRTInviteMeAddAdmin extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                CRTInviteMeAddAdmin
      </div>
        )
    }
}
