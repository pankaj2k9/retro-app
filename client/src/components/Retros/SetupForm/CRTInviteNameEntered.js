import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class CRTInviteNameEntered extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                CRTInviteNameEntered
      </div>
        )
    }
}
