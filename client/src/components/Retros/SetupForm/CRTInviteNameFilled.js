import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class CRTInviteNameFilled extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                CRTInviteNameFilled
      </div>
        )
    }
}
